// Throttling in javascript ensure that a function implements at most once in a given time interval

// for example -> let's suppose that we want to fire a API when user is scrolling something so we don't want to go in hell of calling the api for every single scroll , we need something more better where we can just add a checker and then according to a given specific time the API will be called
